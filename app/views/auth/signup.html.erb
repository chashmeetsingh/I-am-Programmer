Signup

<%= form_for :user,url:{action: "attempt_signup"} do |f| %>
  Username:<%= f.text_field :username%><br><br>
  Email:<%= f.email_field :email %><br><br>
  Password:<%= f.password_field :password %><br><br>
  <%= f.submit "Sign Up", :class => "btn btn-success" %>
<% end %>